import { API_URL } from './env'

/* API url logging for debug
if (!process.browser)
  console.log(
    `From /constants/env.ts: ${API_URL}; From process.env: ${process.env.API_URL}`,
  )
*/

// Here should be endpoints for REST API requests
